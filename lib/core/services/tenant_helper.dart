class TenantHelper {
  static String getCurrentTenant(String emailOrDomain) {
    // Implement your logic:
    // - extract tenant from subdomain
    // - or based on user input
    return 'default'; // fallback tenant
  }
}
